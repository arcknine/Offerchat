%a.chat-item-tab.new{:class => "{{active}} {{new_chat}}" }

  {{#if unread}}
  .message-tick{:class => "{{bounce}}"}
    %span {{unread}}
  {{/if}}

  {{jid}}
  .small-help-block
    Offerchat - Hire an agent