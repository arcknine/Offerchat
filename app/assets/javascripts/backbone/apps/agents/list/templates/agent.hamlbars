.profile-image-container.large
  .profile-image
    %img{:src => ".{{avatar}}"}
  %span.status.online
    %i.icon.icon-check-small
%h3
  {{name}}
.nickname
  {{#unless pending}}{{display_name}}{{/unless}}
  {{#if pending}}Waiting{{/if}}