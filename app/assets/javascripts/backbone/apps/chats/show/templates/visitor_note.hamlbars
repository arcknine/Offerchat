.agent-inline-block
  .profile-image-container
    .profile-image
      %img{ :alt => "Avatar-large", :src => "{{avatar}}" }
.note
  .note-author
    {{user_name}}

    {{#if can_remove}}
    %a.small.warning.delete-note remove
    {{/if}}

  {{message}}
  .date {{created_at}}
