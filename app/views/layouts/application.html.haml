!!!
%html
  %head
    %title Offerchat
    = include_gon
    = stylesheet_link_tag "application", :media => "all"
    = javascript_include_tag "https://js.stripe.com/v1/", "application"
    = csrf_meta_tags
    %meta{ :name=>"stripe-key", :content => STRIPE_PUBLIC_KEY }
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    %link{rel: "icon", type: "image/x-icon", href: "/favicon.ico"}

    - if Rails.env.production?
      = mixpanel_js
    - else
      = mixpanel_dev_js

  %body
    - if current_user.sign_in_count < 2
      .setup-tooltip#first-time-user
        %a.close#first-time-user-close ×
        %h3 Setting Up Your Account
        Please wait while we create your account. You may not receive chats just yet.
        In the mean time, get to know the
        %a{ :href => "https://www.offerchat.com/blog/surprise-treat-for-new-offerchat-users-2/", :target => "_blank" }perks of your Premium trial.

    = yield

    - if Rails.env.production?
      = user_report_js
      / = ofc_js
      = analytics_init

    - if current_user.trial_days_left < 0
      .modal-backdrop
        .modal-window.trial-window
          .modal
            .modal-header
              %a.close.close-trial-end
                ×
              %h2
                Oops! Your 60-Day Premium Trial has ended
            .modal-body
              %p
                The default 60-day period to try Offerchat premium has wrapped up. Continue using it by upgrading your account to enjoy more features like:
              %ul.list.styled
                %li Assign at least 4 agents
                %li No Offerchat logo - use it like it's your own
                %li Dedicated support
                %li SSL security
                %li Quick responses
            .modal-footer.bordered
              %a.btn.trial-pricing{href: "#plans"}
                View Plans and Pricing
              %a.right.hovered.trial-free{style: "margin-top: 7px"}
                Continue using the free plan.
